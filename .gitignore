# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Virtualenv
.venv/
venv/

# PyInstaller build artifacts
build/
dist/
# NOTE: 약정만료 알리미.spec IS committed (it filters api-ms-win-core stubs); do not ignore it.

# Local user data / logs (never in repo)
*.log
